import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#03DAC5",
      contrastText: "#444444",
    },
    secondary: {
      main: "#444444",
    },
  },
});

export default theme;
